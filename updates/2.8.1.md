# Livetracking 2.8.1 — Strecken laufen durch die Schilder

**Datum:** 04.09.2026
**Typ:** Darstellung und Aufräumen, nur Frontend
**Basis:** v2.8.0 („Streckenschilder statt Emoji")
**Repo-Stand:** `codeload.github.com/Rohralarm782/Livetracking/zip/refs/heads/main`,
abgerufen **04.09.2026, 20:45 UTC** (`main.zip` MD5 `66431dd845b51200786523d661674773`)

| Datei | MD5 vorher | MD5 nachher |
|---|---|---|
| `map/gpx.js` | `4a79cb6f347997e1e0314d2224076e64` | `12093ddc9591c9a253a75f7ad9ee2003` |
| `version.json` | `16daaa4d9acfc0d6310743e92f296c04` | `ae9f60effeed709e451397161caa68b3` |

**+34 / −68 Zeilen**, also 34 Zeilen weniger als vorher. Nur zwei Dateien.
Kein `server.js`, kein `css/app.css`, kein `index.html`, kein
`race/events-ui.js`, keine Serverroute, keine Datenbank.

---

## Zweck

Seit 2.6.4 hörten die Streckenlinien unter jedem Marker auf, damit das
Symbol frei stand — das Emoji in der weißen Scheibe deckte die Linien
schlecht, der zusammengelegte Ring maß 32 Pixel. Mit 2.8.0 ist das
Symbol eine deckende 24-Pixel-Scheibe und liegt in Leaflets Markerebene
über den Linien. Die Aussparung hatte damit keinen Zweck mehr und war
nur noch ein sichtbarer Bruch im Streckenverlauf, gut zu sehen an den
Bergwertungen und Sprints der Übersichtskarte.

Ab 2.8.1 laufen die Spuren durch. Ein Nebeneffekt: die Linie ist auf
ihrer ganzen Länge anklickbar — bisher ging ein Tipp im Umkreis von
einigen hundert Metern um jeden Marker ins Leere, weil dort keine Linie
lag.

**Die Zonenlücke bleibt.** Bei Verpflegungs- und Punktzonen tritt das
dicke Zonenband an die Stelle der Linie. Nähme man auch diese Lücke
heraus, läge die Linie unter dem Band und der Effekt kippte.

## Nebeneffekt Rechenzeit

`lueckenPlan()` projizierte für **jede Markergruppe × jedes gezeichnete
Rennen** den Markerpunkt über `sNaechst()` auf die gesamte Strecke. Bei
vier Rennen à 4000 Punkten und zehn Markern waren das rund 160 000
Segmentprojektionen — bei jedem Neuaufbau der Karte und bei jedem
`zoomend`. Dieser Teil entfällt ersatzlos; die verbleibende Zonenliste
kommt ohne Rechnerei aus.

## Bekannter Rest

Die Spuren liegen versetzt (`stufen = [0, 1, -1, 2]` × `VERSATZ_PX` = 5).
Bei **vier** gleichzeitig sichtbaren Rennen reicht das Spurband von
−9 px bis +14 px um die Fahrbahn, das Schild aber nur 12 px nach jeder
Seite: die äußerste Spur des vierten Rennens kann rechts neben dem
Schild ein paar Pixel hervorschauen. Bei einem bis drei Rennen ist alles
gedeckt. Bewusst so belassen (Entscheidung D2a) — erst im Rennen
ansehen, ob es überhaupt auffällt. Zwei Auswege stünden bereit: Schild
auf 26 px, oder das Schild um den Mittelwert des Bandes versetzt setzen.

## Geänderte Stellen im Einzelnen

### 1 — `zeichneStrecken()`, Kommentar und Aufruf

**ALT**

```js
  // Aussparungen. Ein zusammengefasster Markerring misst 32 Pixel, das
  // Spurband bei drei Rennen rund 16 - der Ring deckt die Linien also
  // ohnehin. Statt ihn zu ueberzeichnen, hoeren die Linien dort auf:
  //     ----------(S)----------
  // Die Ringluecke gilt fuer JEDE Spur, die dort vorbeikommt, auch fuer
  // ein Rennen ohne eigenen Marker an der Stelle - sonst laeuft dessen
  // Linie weiter durch das Symbol. Die Zonenluecke gilt nur fuer das
  // Rennen, dem die Zone gehoert: dort tritt die Zone an die Stelle der
  // Linie (siehe drawRaceMarker).
  //
  // Im Streckenmodus wird nichts ausgespart. Dort wird auf die Linie
  // getippt, und eine Luecke waere eine Stelle, an der ein Tipp ins
  // Leere geht.
  const gruppen = (streckenModus && zielRaceId) ? [] : sammleMarker();
  const luecken = lueckenPlan(folge, cum, gruppen);
```

**NEU**

```js
  // Aussparungen. Bis 2.8.0 hoerten die Linien unter jedem Marker auf:
  //     ----------(S)----------
  // Das Symbol war ein Emoji in einer weissen Scheibe und der
  // zusammengelegte Ring 32 Pixel breit - beides deckte die Linien
  // schlecht, also machten die Linien Platz. Seit 2.8.0 ist das Symbol
  // eine deckende Scheibe von 24 Pixeln und liegt in der Markerebene
  // ueber den Linien. Die Spuren laufen deshalb wieder durch; die
  // Luecke war ab da nur noch ein sichtbarer Bruch im Streckenverlauf.
  //
  // Geblieben ist die Zonenluecke: dort tritt das dicke Zonenband an
  // die Stelle der Linie (siehe drawRaceMarker). Sie gilt nur fuer das
  // Rennen, dem die Zone gehoert.
  //
  // Im Streckenmodus wird nichts ausgespart. Dort wird auf die Linie
  // getippt, und eine Luecke waere eine Stelle, an der ein Tipp ins
  // Leere geht.
  const gruppen = (streckenModus && zielRaceId) ? [] : sammleMarker();
  const zonen   = zonenPlan(folge, gruppen);
```

Die Zeile `const gruppen = …` ist unverändert: `sammleMarker()` wird
weiterhin gebraucht, für die Zonenliste und für `drawRaceMarker()`.

### 2 — `zeichneStrecken()`, weiße Kontur

**ALT**

```js
  // Die Kontur kennt nur die Ringluecken, nicht die Zonenluecken: so
  // bekommt die Zone denselben weissen Rand wie die Linie, an deren
  // Stelle sie tritt.
  if (folge.length > 1) {
    folge.forEach(id => {
      gpxKontur[id] = L.polyline(mitLuecken(punkte[id], cum[id], luecken.ring[id]), {
```

**NEU**

```js
  // Die Kontur kennt die Zonenluecken nicht: so bekommt die Zone
  // denselben weissen Rand wie die Linie, an deren Stelle sie tritt.
  // Seit 2.8.1 hat sie damit ueberhaupt keine Luecken mehr und laeuft
  // durch.
  if (folge.length > 1) {
    folge.forEach(id => {
      gpxKontur[id] = L.polyline(punkte[id], {
```

Die drei folgenden Zeilen (`color`, `weight`, `lineJoin`) sind
unverändert.

### 3 — `zeichneStrecken()`, Farblinie

**ALT**

```js
    const linie = L.polyline(
      mitLuecken(punkte[id], cum[id], luecken.ring[id].concat(luecken.zone[id])), {
```

**NEU**

```js
    const linie = L.polyline(
      mitLuecken(punkte[id], cum[id], zonen[id]), {
```

### 4 — Kommentar am `zoomend`-Handler

**ALT**

```js
// auseinander und im Nahzoom wieder deckungsgleich. Dasselbe gilt seit
// 2.6.4 fuer die Aussparungen, deren Breite in Pixeln festgelegt ist.
```

**NEU**

```js
// auseinander und im Nahzoom wieder deckungsgleich.
```

Der Handler selbst (`map.on('zoomend', zeichneStrecken)`) bleibt: die
Spurversätze und die Zonen hängen weiterhin am Zoom.

### 5 — Konstanten und `meterProPixel()` entfernt

**ALT** (28 Zeilen, ersatzlos gestrichen):

```js
// Breite der Aussparung unter einem Marker, in Bildschirmpixeln. Der
// zusammengefasste Ring misst 32 Pixel, das einzelne Symbol 24. Die
// Luecke ist bewusst deutlich breiter als das Symbol: sie soll den
// Marker frei stellen, nicht nur knapp umschliessen - sonst kleben die
// Linien an seinem Rand und das Symbol geht im Band unter.
const LUECKE_RING_PX  = 56;
const LUECKE_PUNKT_PX = 46;

// Deckel, damit im weit herausgezoomten Bild nicht ein halber Rundkurs
// verschwindet: hoechstens zwei Prozent der Streckenlaenge, bei einem
// 100-km-Rennen also zwei Kilometer. In den Zoomstufen, in denen im
// Auto gearbeitet wird, greift der Deckel nie - dort sind 56 Pixel
// einige hundert Meter. Der Mindestwert haelt die Luecke auch bei
// einem kurzen Rundkurs brauchbar.
const LUECKE_MAX_ANTEIL = 0.02;
const LUECKE_MIN_M      = 250;

// Wie nah ein Rennen an einem Marker vorbeikommen muss, damit seine
// Spur ausgespart wird. Faehrt es weiter weg, deckt der Ring seine
// Linie gar nicht.
const LUECKE_NAH_M = 60;

// Wie viele Meter ein Bildschirmpixel im aktuellen Zoom entspricht.
function meterProPixel() {
  const breite = 40075016.686 * Math.cos(map.getCenter().lat * Math.PI / 180);
  return breite / (256 * Math.pow(2, map.getZoom()));
}
```

Geprüft: keiner dieser fünf Namen und keine Referenz auf
`meterProPixel` kommt im Repo noch vor. `VERSATZ_PX` und `GLAETTUNG_PX`
sind davon nicht betroffen — die gehören zum Spurversatz und bleiben.

### 6 — Kommentar an `sNaechst()`

**ALT**

```js
// naechste Stuetzpunkt laege dann hunderte Meter entfernt und die
// Aussparung fiele still aus.
```

**NEU**

```js
// naechste Stuetzpunkt laege dann hunderte Meter entfernt und die
// Liste der naechsten Punkte zeigte still auf die falsche Stelle.
```

`sNaechst()` selbst bleibt **unverändert** — sie wird von
`naechstePunkteAb()` gebraucht, dem Bezugspunkt der Liste links auf der
Karte.

### 7 — `lueckenPlan()` wird `zonenPlan()`

**ALT**

```js
// Je Rennen zwei Listen von Sperrbereichen in Metern:
//   ring - unter jedem Markersymbol, fuer jede Spur, die dort
//          vorbeikommt
//   zone - der Abschnitt einer Verpflegungs- oder Punktzone, nur fuer
//          das Rennen, dem sie gehoert
function lueckenPlan(ids, cum, gruppen) {
  const plan = { ring: Object.create(null), zone: Object.create(null) };
  ids.forEach(id => { plan.ring[id] = []; plan.zone[id] = []; });
  if (!gruppen || !gruppen.length) return plan;

  const mpp = meterProPixel();
  gruppen.forEach(g => {
    const gross = g.eintraege.length > 1;
    const roh = (gross ? LUECKE_RING_PX : LUECKE_PUNKT_PX) / 2 * mpp;
    ids.forEach(id => {
      const t = sNaechst(g.lat, g.lon, gpxByRace[id] || [], cum[id] || []);
      if (!t || t.dist > LUECKE_NAH_M) return;
      // Der Deckel haengt an der Laenge DIESER Strecke: zwei Rennen mit
      // sehr verschiedener Distanz sollen im selben Bild nicht
      // unterschiedlich stark ausgespart werden, solange beide weit
      // genug sind.
      const c   = cum[id];
      const max = Math.max(LUECKE_MIN_M, (c[c.length - 1] || 0) * LUECKE_MAX_ANTEIL);
      const halb = Math.min(roh, max / 2);
      plan.ring[id].push([t.s - halb, t.s + halb]);
    });
    // Zonen stehen in denselben Gruppen - so wird die Markerliste nur
    // einmal durchlaufen.
    if (!markerArt(g.typ).zone) return;
    g.eintraege.forEach(e => {
      if (e.m.sEnde === undefined || e.m.sEnde === null) return;
      if (!plan.zone[e.raceId]) return;
      plan.zone[e.raceId].push([e.m.s, e.m.sEnde]);
    });
  });
  return plan;
}
```

**NEU**

```js
// Je Rennen eine Liste von Sperrbereichen in Metern: die Abschnitte
// seiner Verpflegungs- und Punktzonen. Dort tritt das Zonenband an die
// Stelle der Linie, also muss die Linie weichen.
//
// Bis 2.8.0 stand hier zusaetzlich ein Ringteil, der unter jedem
// Markersymbol jede vorbeikommende Spur aussparte. Er ist mit 2.8.1
// entfallen - siehe den Kommentar in zeichneStrecken(). Mit ihm faellt
// eine Projektion je Markergruppe und Rennen ueber alle Streckenpunkte
// weg, die bei jedem Zoomwechsel neu lief.
function zonenPlan(ids, gruppen) {
  const plan = Object.create(null);
  ids.forEach(id => { plan[id] = []; });
  if (!gruppen || !gruppen.length) return plan;

  gruppen.forEach(g => {
    if (!markerArt(g.typ).zone) return;
    g.eintraege.forEach(e => {
      if (e.m.sEnde === undefined || e.m.sEnde === null) return;
      if (!plan[e.raceId]) return;
      plan[e.raceId].push([e.m.s, e.m.sEnde]);
    });
  });
  return plan;
}
```

Die Rückgabe ist flacher: statt `{ ring: {…}, zone: {…} }` jetzt direkt
`{ raceId: [[von, bis], …] }`. Der Parameter `cum` entfällt, er wurde
nur für den Deckel des Ringteils gebraucht. `mitLuecken()` und
`punktBei()` sind **unverändert** — sie verarbeiten die Zonenliste
genauso wie vorher die kombinierte Liste.

### 8 — `version.json`

**ALT**

```json
  "version": "2.8.0",
  "date": "2026-09-04",
  "title": "Streckenschilder statt Emoji"
```

**NEU**

```json
  "version": "2.8.1",
  "date": "2026-09-04",
  "title": "Strecken laufen durch die Schilder"
```

## Verifikation vor Auslieferung

| Prüfung | Ergebnis |
|---|---|
| `node --check map/gpx.js` | bestanden |
| Express-Routen in `server.js` | 68 vorher, 68 nachher (Datei unberührt) |
| Funktionsnamen `map/gpx.js` | `lueckenPlan` und `meterProPixel` entfallen, `zonenPlan` neu — sonst identisch |
| Restsuche nach alten Namen | `luecken.ring`, `luecken.zone`, `lueckenPlan`, `meterProPixel` und alle fünf `LUECKE_*` je 0 Treffer |
| jsdom-Test, 15 Prüfungen | alle bestanden |
| jsdom-Test aus 2.8.0 (30 Prüfungen) | erneut gelaufen, alle bestanden |

Inhalt des neuen jsdom-Tests: Berg- und Sprintgruppen sperren nichts
mehr; eine Verpflegungszone sperrt genau ihren Abschnitt von `s` bis
`sEnde` und nur für das eigene Rennen; eine Zone ohne `sEnde` sperrt
nichts; leere und `null`-Gruppen sind abgefangen; ein nicht
gezeichnetes Rennen legt keinen Eintrag an; `mitLuecken()` gibt bei
leerer Sperrliste die Punktliste unverändert zurück und zerlegt sie bei
einem Sperrbereich in zwei Teilstücke; die Kontur bekommt im Quelltext
die vollen Punkte, die Farblinie nur die Zonenliste; `sNaechst()` ist
noch vorhanden.

## Breaking Changes

Keine. Kein Datenformat, keine Route, kein gespeicherter Wert. Rein
zeichnerisch.

## Rollback

`map/gpx.js` und `version.json` aus dem Stand 2.8.0 zurückspielen. Kein
Datenbankschritt, kein Serverneustart. Der Service Worker zieht den
alten Cache-Namen automatisch, sobald `version.json` zurück auf `2.8.0`
steht.
