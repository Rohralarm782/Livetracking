# Livetracking 2.8.0 — Streckenschilder statt Emoji

**Datum:** 04.09.2026
**Typ:** Darstellung, nur Frontend
**Basis:** v2.7.0 („Nächste Punkte auf der Karte")
**Repo-Stand:** `codeload.github.com/Rohralarm782/Livetracking/zip/refs/heads/main`,
abgerufen **04.09.2026, 20:26 UTC** (`main.zip` MD5 `3e72ad4881b42afa678f7dec6c7aef4d`)

| Datei | MD5 vorher | MD5 nachher |
|---|---|---|
| `map/gpx.js` | `56b0908a1345791469953e82dad74da5` | `4a79cb6f347997e1e0314d2224076e64` |
| `css/app.css` | `25874665b880606bd82aa91611348a9c` | `26ad668efb57edbfce242f141b3fb284` |
| `race/events-ui.js` | `0f737d3f00c6b72867b0f5ffd096567c` | `fe316390fce651845faca6c41527b243` |
| `version.json` | `4e6b2daee4067b285b0ad25414749fdc` | `16daaa4d9acfc0d6310743e92f296c04` |

**+67 / −49 Zeilen.** Kein `server.js`, keine Serverroute, keine Datenbank,
kein `race/favorites.js`, keine Firmware, kein `index.html`.

---

## Zweck

Sprint, Bergwertung und Verpflegungszone tragen auf der Karte ab sofort
das Schild, das auch am Streckenrand steht: grünes S, rotes Bergschild,
blaues Besteck. Bisher standen dort Emoji (🏅 ⛰️ 🍌), die auf jedem
Gerät anders gerendert werden und aus dem fahrenden Auto schlecht zu
erkennen sind.

Der Farbring um zusammengelegte Punkte entfällt. Er zeigte, für welche
Rennen ein Punkt gilt — eine Information, die im Rennen nicht
handlungsleitend ist, weil Wertungen fast immer für alle Rennen an
derselben Stelle liegen und sich nur im Kilometerstand unterscheiden.
Genau diese Kilometer stehen unverändert zeilenweise im Tooltip.

Zwischenzeit und freier Punkt behalten ihr Emoji in der weißen Scheibe.
Das ist beabsichtigt: sie sind keine Wertung, sondern eine eigene Notiz,
und die Unterscheidung ist auf der Karte sofort sichtbar.

## Wo die Schilder erscheinen

- Kartenmarker (`markerIcon`)
- Liste „nächste Punkte" links auf der Karte (seit 2.7.0)
- Punkteliste und Typ-Knöpfe im Streckeneditor der Rennverwaltung

Nicht betroffen sind alle reinen Textstellen — Tooltips, Toasts, die
Überschrift im Streckenmodus und die Zeile „nächster Punkt" in der
Rennleiste. Dort landet der Wert als Text im DOM, ein SVG würde als
roher Quelltext erscheinen. Diese Stellen zeigen weiterhin `a.icon`.

## Geänderte Dateien im Einzelnen

### 1 — `map/gpx.js`, Schilddefinitionen (vor `const MARKER_ART`)

**NEU** (32 Zeilen eingefügt, nichts entfernt):

```js
// Streckenschilder fuer Sprint, Bergwertung und Verpflegung. Eigene
// Zeichnung statt Emoji: Emoji rendern auf jedem Geraet anders und sind
// aus dem fahrenden Auto schlecht zu erkennen. Die drei Zeichen folgen
// der Beschilderung am Streckenrand - gruenes S, rotes Bergschild,
// blaues Besteck. Das Markup ist fest verdrahtet, es kommt kein
// Benutzertext hinein. Zwischenzeit und freier Punkt behalten ihr
// Emoji: sie sind keine Wertung, sondern eine eigene Notiz.
const MK_SYM_KOPF = '<svg class="lt-sym" viewBox="0 0 24 24" width="24" height="24" aria-hidden="true">';
const MK_SYM_SPRINT = MK_SYM_KOPF
  + '<circle cx="12" cy="12" r="10.6" fill="#2e9b4f" stroke="#fff" stroke-width="2"/>'
  + '<text x="12" y="17" text-anchor="middle" font-family="Arial, Helvetica, sans-serif"'
  + ' font-size="14" font-weight="700" fill="#fff">S</text></svg>';
const MK_SYM_BERG = MK_SYM_KOPF
  + '<circle cx="12" cy="12" r="10.6" fill="#d62828" stroke="#fff" stroke-width="2"/>'
  + '<path d="M5 16.8 L9.9 8.4 L12.9 12.4 L15 9.6 L19 16.8 Z" fill="#fff"/></svg>';
const MK_SYM_FOOD = MK_SYM_KOPF
  + '<circle cx="12" cy="12" r="10.6" fill="#1976d2" stroke="#fff" stroke-width="2"/>'
  + '<path d="M8.2 6.2v3.2M10 6.2v3.2M11.8 6.2v3.2" stroke="#fff" stroke-width="1.1" stroke-linecap="round"/>'
  + '<path d="M7.9 9.1h4.2v.9a2.1 2.1 0 0 1-4.2 0Z" fill="#fff"/>'
  + '<path d="M10 11.8v6" stroke="#fff" stroke-width="1.5" stroke-linecap="round"/>'
  + '<path d="M15.4 6.1c2.1 1.1 2.1 4.9 0 5.9Z" fill="#fff"/>'
  + '<path d="M15.4 11.6v6.2" stroke="#fff" stroke-width="1.5" stroke-linecap="round"/></svg>';
```

### 2 — `map/gpx.js`, drei Einträge in `MARKER_ART`

**ALT**

```js
  wertung:     { icon: '\u{1F3C5}', label: 'Sprint',       farbe: '#1565c0', zone: false },
  berg:        { icon: '\u26F0\uFE0F', label: 'Bergwertung', farbe: '#6d4c41', zone: false },
  verpflegung: { icon: '\u{1F34C}', label: 'Verpflegung',  farbe: '#ef6c00', zone: true  },
```

**NEU**

```js
  wertung:     { icon: '\u{1F3C5}', svg: MK_SYM_SPRINT, label: 'Sprint',       farbe: '#1565c0', zone: false },
  berg:        { icon: '\u26F0\uFE0F', svg: MK_SYM_BERG, label: 'Bergwertung', farbe: '#6d4c41', zone: false },
  verpflegung: { icon: '\u{1F34C}', svg: MK_SYM_FOOD, label: 'Verpflegung',  farbe: '#ef6c00', zone: true  },
```

`icon` bleibt in allen drei Zeilen unverändert stehen — es wird von den
Textstellen weiter gebraucht. `start`, `frei` und `zwischenzeit` sind
byte-identisch geblieben.

### 3 — `map/gpx.js`, neue Funktion nach `markerArt()`

**NEU** (9 Zeilen eingefügt, nichts entfernt):

```js
// Das Zeichen fuer eine Flaeche: Schild, wo es eines gibt, sonst das
// Emoji. Nur fuer Stellen, an denen HTML erlaubt ist - in Tooltips,
// Toasts und Ueberschriften bleibt a.icon stehen, dort landet der Wert
// als Text.
function markerSymbol(typ) {
  const a = markerArt(typ);
  return a.svg || a.icon;
}
```

### 4 — `map/gpx.js`, `naechstePunkteAb()`

**ALT**

```js
    if (!drin) roh.push({ icon: a.icon, d: vor(m.s),     ende: false, runden: m.runden });
    if (zone)  roh.push({ icon: a.icon, d: vor(m.sEnde), ende: true,  runden: m.runden });
```

**NEU**

```js
    const sym  = a.svg || a.icon;
    if (!drin) roh.push({ icon: sym, d: vor(m.s),     ende: false, runden: m.runden });
    if (zone)  roh.push({ icon: sym, d: vor(m.sEnde), ende: true,  runden: m.runden });
```

`map/map.js` bleibt dadurch byte-identisch: `zeichneNaechstePunkte()`
setzt `e.icon` unverändert ins `innerHTML`, es kommt nur ein anderer
Wert an. Der Kommentar dort („Die Symbole stammen aus MARKER_ART, nicht
aus einer Eingabe") gilt weiterhin.

### 5 — `map/gpx.js`, `markerIcon()` und Wegfall von `markerIconRing()`

**ALT**

```js
function markerIcon(typ) {
  const a = markerArt(typ);
  return L.divIcon({
    className: '', iconSize: [24, 24], iconAnchor: [12, 12],
    html: `<div class="lt-mk" style="border-color:${a.farbe}">${a.icon}</div>`
  });
}

// Ein Punkt, der fuer mehrere Rennen gilt: ein Symbol, aussen ein Ring
// aus einem Segment je Rennen. Bei genau einem Rennen ergibt das einen
// einfachen Farbring - die Darstellung bleibt damit fast so, wie sie
// bis 1.19.0 war.
function markerIconRing(typ, farben) {
  const a = markerArt(typ);
  if (!farben || farben.length < 2) {
    return L.divIcon({
      className: '', iconSize: [24, 24], iconAnchor: [12, 12],
      html: `<div class="lt-mk" style="border-color:${farben && farben[0] ? farben[0] : a.farbe}">${a.icon}</div>`
    });
  }
  const R = 15, C = 16, dick = 4;
  const u = 2 * Math.PI * R;
  const seg = farben.map((f, i) => {
    const teil = u / farben.length;
    return `<circle cx="${C}" cy="${C}" r="${R}" fill="none" stroke="${f}"
      stroke-width="${dick}" stroke-dasharray="${teil - 1.5} ${u - teil + 1.5}"
      stroke-dashoffset="${-i * teil}" transform="rotate(-90 ${C} ${C})"/>`;
  }).join('');
  return L.divIcon({
    className: '', iconSize: [32, 32], iconAnchor: [16, 16],
    html: `<div class="lt-mkr"><svg width="32" height="32" viewBox="0 0 32 32">${seg}</svg>`
        + `<span class="lt-mkr-i">${a.icon}</span></div>`
  });
}
```

**NEU**

```js
// Bis 2.7.0 trug ein Punkt, der fuer mehrere Rennen gilt, aussen einen
// Ring aus einem Segment je Rennen (markerIconRing). Der ist mit 2.8.0
// entfallen: die Wertungen liegen fast immer fuer alle Rennen an
// derselben Stelle, nur bei verschiedenen Kilometern - und genau die
// stehen weiterhin zeilenweise im Tooltip (clusterText). Der Ring hat
// dafuer 8 px Durchmesser und den Kontrast des Schildes gekostet.
function markerIcon(typ) {
  const a = markerArt(typ);
  return L.divIcon({
    className: '', iconSize: [24, 24], iconAnchor: [12, 12],
    html: a.svg || `<div class="lt-mk" style="border-color:${a.farbe}">${a.icon}</div>`
  });
}
```

### 6 — `map/gpx.js`, Aufrufstelle in `drawRaceMarker()`

**ALT**

```js
  gruppen.forEach(g => {
    const farben = g.eintraege.map(e => rennFarbe(e.raceId));
    markerLayer.push(
      L.marker([g.lat, g.lon], { icon: markerIconRing(g.typ, farben), interactive: false })
```

**NEU**

```js
  gruppen.forEach(g => {
    markerLayer.push(
      L.marker([g.lat, g.lon], { icon: markerIcon(g.typ), interactive: false })
```

Gruppierung (`sammleMarker`, `MK_CLUSTER_M`, `MK_CLUSTER_GRAD`) und
Tooltip (`clusterText`) sind unverändert. `rennFarbe()` wird an dieser
Stelle nicht mehr gebraucht, an allen anderen (Zonenbänder, Tooltip)
schon.

### 7 — `css/app.css`, Ring-Regeln entfernt

**ALT** (elf Zeilen inkl. Leerzeile, ersatzlos gestrichen):

```css
    /* ZUSAMMENGELEGTER STRECKENPUNKT - ein Symbol, aussen ein
       Ringsegment je Rennen. */
    .lt-mkr { position: relative; width: 32px; height: 32px; }
    .lt-mkr svg { position: absolute; inset: 0; }
    .lt-mkr-i {
      position: absolute; inset: 0; display: flex;
      align-items: center; justify-content: center;
      font-size: 13px; line-height: 1;
    }
    .leaflet-marker-icon.lt-mkr, .lt-mkr { background: none; border: none; }
```

Die direkt folgenden Regeln `.lt-tt .lt-tt-h` und `.lt-tt .lt-tt-r`
(Tooltip des zusammengelegten Punktes) bleiben stehen — der Tooltip
bleibt.

### 8 — `css/app.css`, neue Regeln für `.lt-sym`

**ALT**

```css
    /* Symbol eines Streckenpunkts auf der Karte */
    .lt-mk {
```

**NEU**

```css
    /* Symbol eines Streckenpunkts auf der Karte. Die weisse Scheibe
       tragen seit 2.8.0 nur noch Zwischenzeit und freier Punkt -
       Sprint, Berg und Verpflegung sind gezeichnete Schilder (.lt-sym). */
    .lt-mk {
```

Der Regelinhalt von `.lt-mk` selbst ist unverändert. Danach eingefügt:

```css
    /* Gezeichnetes Streckenschild. Der Schatten kommt aus dem Filter und
       nicht aus box-shadow: das Schild ist rund, der Kasten drumherum
       quadratisch. */
    .lt-sym { display: block; filter: drop-shadow(0 1px 2px rgba(0,0,0,.45)); }
    /* In der Punkteliste und im Streckeneditor steht dasselbe Schild
       kleiner neben dem Text. */
    #naechstePunkte .npI .lt-sym { width: 17px; height: 17px; margin: 0 auto; }
    #naechstePunkte .npZ.spaeter .lt-sym { opacity: .55; }
    .mk-zeile .mk-icon .lt-sym { width: 18px; height: 18px; }
    .mk-typen .btn .lt-sym { display: inline-block; width: 15px; height: 15px; vertical-align: -3px; }
```

### 9 — `race/events-ui.js`, neue Funktion `mkSym()`

**NEU** (7 Zeilen eingefügt zwischen `mkArt()` und `mkKm()`):

```js
// Dasselbe Zeichen wie auf der Karte. markerSymbol() steht in gpx.js,
// das vor dieser Datei geladen wird; der Rueckfall greift nur, falls
// die Reihenfolge einmal kippt.
function mkSym(a) {
  return (typeof markerSymbol === 'function') ? markerSymbol(a.typ) : a.icon;
}
```

`MK_ARTEN` bleibt vollständig unverändert — die Schilder werden nicht
ein zweites Mal definiert, sondern aus `gpx.js` geholt.

### 10 — `race/events-ui.js`, Punkteliste

**ALT**

```js
      <span class="mk-icon">${a.icon}</span>
```

**NEU**

```js
      <span class="mk-icon">${mkSym(a)}</span>
```

### 11 — `race/events-ui.js`, Typ-Knöpfe im Formular

**ALT**

```js
            style="padding:5px 9px;font-size:12px">${x.icon} ${x.label}</button>`).join('')}
```

**NEU**

```js
            style="padding:5px 9px;font-size:12px">${mkSym(x)} ${x.label}</button>`).join('')}
```

Der Toast nach dem Speichern (`showToast(...)`, Zeile 723 im alten
Stand) bleibt bewusst beim Emoji: `showToast()` setzt Text, kein HTML.

### 12 — `version.json`

**ALT**

```json
  "version": "2.7.0",
  "date": "2026-09-04",
  "title": "Nächste Punkte auf der Karte"
```

**NEU**

```json
  "version": "2.8.0",
  "date": "2026-09-04",
  "title": "Streckenschilder statt Emoji"
```

`sw.js` liest den Cache-Namen aus `/version` und braucht keine Änderung.

## Verifikation vor Auslieferung

| Prüfung | Ergebnis |
|---|---|
| `node --check map/gpx.js` | bestanden |
| `node --check race/events-ui.js` | bestanden |
| Express-Routen in `server.js` | 68 vorher, 68 nachher (Datei unberührt) |
| Funktionsnamen `map/gpx.js` | `markerIconRing` entfällt (Absicht, Hunk 5), `markerSymbol` neu |
| Funktionsnamen `race/events-ui.js` | `mkSym` neu, sonst identisch |
| jsdom-Test, 30 Prüfungen | alle bestanden |
| Puppeteer-Rendering 24 px / 17 px | Schilder lesbar, Zonenende-Strich sitzt |

Inhalt des jsdom-Tests: Schild nur für `wertung`/`berg`/`verpflegung`,
Emoji für `zwischenzeit`/`frei`/`start`, Rückfall auf `frei` bei
unbekanntem Typ, `markerIcon` liefert 24 px mit Anker mittig,
`markerIconRing` existiert nicht mehr, jedes Schild ist gültiges SVG mit
Klasse `lt-sym`, `markerBeschriftung()` enthält weiterhin kein Markup,
`mkSym()` im Editor liefert dasselbe Zeichen und fällt ohne `gpx.js`
sauber auf das Emoji zurück.

## Breaking Changes

Keine. Kein Datenformat, keine Route, kein gespeicherter Wert ändert
sich. `marker.typ` bleibt unverändert; ältere Clients zeigen weiterhin
Emoji.

## Rollback

Vier Dateien aus dem Stand 2.7.0 zurückspielen:
`map/gpx.js`, `css/app.css`, `race/events-ui.js`, `version.json`.
Kein Datenbankschritt, kein Serverneustart nötig. Der Service Worker
zieht den alten Cache-Namen automatisch wieder, sobald `version.json`
zurück auf `2.7.0` steht.
