# 2.13.0 — Bezugspunkt Teamauto

**Datum:** 05.09.2026
**Aufgesetzt auf:** Commit `a3fc2aaf`, gezogen am 05.09.2026 20:28 UTC über
`codeload.github.com/Rohralarm782/Livetracking/zip/refs/heads/main`
**Betroffene Dateien:** `map/map.js`, `map/gpx.js`, `version.json`
**Breaking Changes:** keine
**Rollback:** Die drei Dateien aus Commit `a3fc2aaf` zurückspielen. Es gibt
keinen Serveranteil, kein Datenbankschema und keinen persistierten Zustand —
der Rückbau ist ein reiner Dateiaustausch. `version.json` fällt dabei auf
2.12.0 zurück, der Service Worker legt seinen Cache dann wieder unter dem
alten Namen an.

---

## Warum dieses Update

### 1. Die Liste „Nächste Punkte" blieb leer, obwohl das Teamauto auf der Karte stand

Am 05.09.2026 meldete die Karte dauerhaft **„Keine Teamauto-Position"**, während
der Wagen als Marker `MEV` mit Auto-Symbol sichtbar war.

Ursache: `npBezugsPunkt()` in `map/map.js` kannte genau zwei Quellen — die
eigene Browser-Ortung (`teamCarMarker`) und den Eintrag unter dem festen
Schlüssel `TEAMAUTO`, den der SpoLei-Browser über `POST /team-position`
schreibt. Die native Android-App sendet aber über die normale Tracker-Route
unter ihrer eigenen Kennung. Dass ihr Marker das Auto-Symbol trug, kommt aus
dem Feld `role: 'teamauto'` (Serverroute `/tracker-role`) — und dieses Feld
wurde von `npBezugsPunkt()` nie ausgewertet.

Marker sichtbar, Rolle korrekt, Liste trotzdem leer.

**Nicht die Ursache** war die Entfernung zur Strecke. `NP_ABSEITS_M = 250`
führt nicht zum Ausblenden, sondern zur Kopfzeile „x km neben der Strecke".

### 2. Am Schreibtisch war Platz für mehr Zeilen

`NP_ANZAHL` stand fest auf 5 — sinnvoll im fahrenden Auto, verschenkt am
Laptop.

---

## Wie gewählt wird, wenn mehrere Wagen die Rolle Teamauto haben

Drei Stufen, in dieser Reihenfolge:

1. **Eigene Ortung** (`teamCarMarker`), solange sie läuft
2. **`TEAMAUTO`** aus `/team-position`
3. **Neu:** ein Tracker mit `role === 'teamauto'`

Auf Stufe 3 fällt die Wahl **einmal** und bleibt dann bestehen, solange der
gewählte Wagen frisch meldet und seine Rolle behält. Erst bei Ausfall wird neu
gewählt.

Der Grund für dieses Gedächtnis: Jedes rein gemessene Kriterium flackert. Im
Konvoi liegt mal der eine Wagen bei 6 m und mal der andere bei 4 m von der
Linie entfernt — ohne Festhalten würde der Bezugspunkt im Sekundentakt
wechseln und die Kilometerzahlen um hunderte Meter springen.

Die **Erstwahl** trifft den kleinsten Abstand zur Strecke des eigenen Rennens.
Bewusst nicht die Streckenposition: auf einem Rundkurs — Bad Dürrheim ist
einer — hat der Führende direkt hinter dem Zielstrich `s ≈ 0` und wäre damit
rechnerisch der Hinterste. Der Abstand hat dieses Problem nicht und schließt
zugleich den Wagen aus, der über die Bundesstraße zum Ziel vorfährt.

Ist keine Strecke geladen, greift als Rückfall die jüngste Meldung.

---

## Zeilenanzahl

| Fenster | Zeilen |
|---|---|
| schmaler als 768 px | 5 (unverändert) |
| ab 768 px, nach verfügbarer Höhe | 5 bis 8 |

Gerechnet wird bei jedem Zeichnen neu. Ein `resize`-Horcher war deshalb nicht
nötig: Drehen des Geräts oder Verkleinern des Fensters wirkt beim nächsten
Takt. Die Schwelle 768 px ist dieselbe wie die bestehende Media Query in
`css/app.css` — es kommt keine zweite Grenze ins System.

`css/app.css` wurde **nicht** angefasst. Der Kasten ist `width: max-content`
und wächst ohne weiteres nach unten.

---

# Hunks im Einzelnen

## `map/map.js`

### Hunk 1 — Beschriftung des Schalters (Zeile 666)

Die feste Zahl im Text wäre falsch geworden.

**ALT**
```js
    ? 'Links auf der Karte: Entfernung bis zu den n\u00E4chsten f\u00FCnf Punkten des eigenen Rennens.'
```

**NEU**
```js
    ? 'Links auf der Karte: Entfernung bis zu den n\u00E4chsten Punkten des eigenen Rennens. Am gro\u00DFen Bildschirm mehr Zeilen als am Handy.'
```

---

### Hunk 2 — `npBezugsPunkt()` (ab Zeile 670)

**ALT** (13 Zeilen Code, 10 Zeilen Kommentar)
```js
// Bezugspunkt der Rechnung.
//
// Erste Wahl ist die eigene Ortung, solange sie laeuft: sie ist eine
// Sekunde frisch statt einen Abfragetakt alt. Sonst die Position, die
// der SpoLei sendet - sie markiert, wo das Rennen ist, und steht in
// /positions jedem Geraet zur Verfuegung.
//
// lastPosData wird in race/taktik.js mit let deklariert, und das laedt
// nach map.js. Ein Zugriff vor dem Laden faellt in die temporale Tote
// Zone und wirft - auch bei typeof. Deshalb try/catch statt Abfrage.
function npBezugsPunkt() {
  if (teamCarMarker) {
    const p = teamCarMarker.getLatLng();
    return [p.lat, p.lng];
  }
  let daten = null;
  try { daten = lastPosData; } catch (e) { return null; }
  const p = daten ? daten['TEAMAUTO'] : null;
  if (!p || typeof p.lat !== 'number' || typeof p.lon !== 'number') return null;
  // Eine Stunde alte Position waere schlimmer als keine: sie sieht aus
  // wie eine Aussage und ist keine.
  if (p.timestamp && Date.now() - p.timestamp > STALE_MS) return null;
  return [p.lat, p.lon];
}
```

**NEU**
```js
// Der einmal gewaehlte Tracker bleibt der Bezugspunkt, solange er
// frisch meldet. Ohne dieses Gedaechtnis wechselt der Bezug im Konvoi
// bei jedem Takt zwischen zwei Wagen hin und her, weil mal der eine und
// mal der andere naeher an der Linie liegt - die Kilometerzahlen
// springen dann um hunderte Meter, ohne dass sich etwas geaendert hat.
let npBezugId = null;

// Ein Eintrag aus /positions, wenn er brauchbar ist. Eine veraltete
// Position waere schlimmer als keine: sie sieht aus wie eine Aussage
// und ist keine. Grenze ist STALE_MS, dieselbe wie beim Ausgrauen der
// Marker.
function npFrisch(daten, id) {
  const p = daten ? daten[id] : null;
  if (!p || typeof p.lat !== 'number' || typeof p.lon !== 'number') return null;
  if (p.timestamp && Date.now() - p.timestamp > STALE_MS) return null;
  return p;
}

// Bezugspunkt der Rechnung.
//
// Erste Wahl ist die eigene Ortung, solange sie laeuft: sie ist eine
// Sekunde frisch statt einen Abfragetakt alt. Sonst die Position, die
// der SpoLei ueber /team-position sendet - sie liegt unter dem
// Schluessel TEAMAUTO und steht jedem Geraet zur Verfuegung.
//
// Ab 2.13.0 als dritte Stufe jeder Tracker mit der Rolle Teamauto. Die
// native App sendet unter ihrer eigenen Kennung, nicht als TEAMAUTO -
// ihre Position blieb damit bis 2.12.0 ungenutzt, und der Kasten
// meldete "Keine Teamauto-Position", obwohl der Wagen auf der Karte
// stand.
//
// lastPosData wird in race/taktik.js mit let deklariert, und das laedt
// nach map.js. Ein Zugriff vor dem Laden faellt in die temporale Tote
// Zone und wirft - auch bei typeof. Deshalb try/catch statt Abfrage.
function npBezugsPunkt() {
  if (teamCarMarker) {
    const p = teamCarMarker.getLatLng();
    return [p.lat, p.lng];
  }
  let daten = null;
  try { daten = lastPosData; } catch (e) { return null; }

  const p = npFrisch(daten, 'TEAMAUTO');
  if (p) return [p.lat, p.lon];

  // Die bestehende Wahl zuerst - sie gilt weiter, solange der Wagen
  // meldet und seine Rolle behalten hat.
  const klebt = npBezugId ? npFrisch(daten, npBezugId) : null;
  if (klebt && klebt.role === 'teamauto') return [klebt.lat, klebt.lon];
  npBezugId = null;

  // Erstwahl: der Wagen mit dem kleinsten Abstand zur Strecke des
  // eigenen Rennens. Laeuft nur, solange keiner gewaehlt ist - die
  // Projektion ueber alle Streckenpunkte je Kandidat soll nicht in
  // jedem Takt anfallen.
  const rid = (typeof meinRaceId === 'function') ? meinRaceId() : null;
  let wahl = null, besterAbstand = Infinity;
  let ersatz = null, ersatzZeit = -1;
  for (const id of Object.keys(daten || {})) {
    const k = npFrisch(daten, id);
    if (!k || k.role !== 'teamauto') continue;
    // Rueckfall, falls keine Strecke geladen ist: die juengste Meldung.
    const zeit = k.timestamp || 0;
    if (zeit > ersatzZeit) { ersatzZeit = zeit; ersatz = id; }
    const d = (rid && typeof npStreckenAbstand === 'function')
      ? npStreckenAbstand(k.lat, k.lon, rid) : null;
    if (d !== null && d < besterAbstand) { besterAbstand = d; wahl = id; }
  }
  npBezugId = wahl || ersatz;
  if (!npBezugId) return null;
  const w = daten[npBezugId];
  return [w.lat, w.lon];
}
```

**Was erhalten bleibt:** Rangfolge eigene Ortung → `TEAMAUTO` → Rolle. Die
`try/catch`-Absicherung für `lastPosData` und die `STALE_MS`-Prüfung sind
unverändert, letztere nur in `npFrisch()` ausgelagert.

---

## `map/gpx.js`

### Hunk 3 — Zeilenanzahl (Zeile 684)

**ALT**
```js
const NP_ANZAHL     = 5;     // so viele Zeilen
```

**NEU**
```js
// Zeilenanzahl. Im Auto zaehlt der schnelle Blick, und der Kasten darf
// den Kartenausschnitt auf dem Handy nicht zustellen - dort bleibt es
// bei fuenf. Am Schreibtisch ist Platz da, und wer plant, will weiter
// nach vorne sehen als bis zur naechsten Verpflegung.
//
// Gerechnet wird bei jedem Zeichnen neu, deshalb braucht es keinen
// resize-Horcher: Drehen des Geraets oder Verkleinern des Fensters
// wirkt beim naechsten Takt.
const NP_ANZAHL_MIN =   5;   // Handy und schmale Fenster
const NP_ANZAHL_MAX =   8;   // mehr wird auch am Schreibtisch nicht gelesen
const NP_BREIT_PX   = 768;   // dieselbe Schwelle wie die Media Query im Stilblatt
const NP_ZEILE_PX   =  25;   // gemessene Zeilenhoehe inklusive Trennlinie
const NP_OBEN_PX    = 124;   // Oberkante des Kastens, siehe #naechstePunkte
const NP_UNTEN_PX   =  40;   // Luft zur Fussleiste von Leaflet

function npAnzahl() {
  if (typeof window === 'undefined') return NP_ANZAHL_MIN;
  if ((window.innerWidth || 0) < NP_BREIT_PX) return NP_ANZAHL_MIN;
  const platz = Math.floor(
    ((window.innerHeight || 0) - NP_OBEN_PX - NP_UNTEN_PX) / NP_ZEILE_PX);
  if (!isFinite(platz)) return NP_ANZAHL_MIN;
  return Math.max(NP_ANZAHL_MIN, Math.min(NP_ANZAHL_MAX, platz));
}
```

Die Konstanten `NP_ABSEITS_M`, `NP_RUNDKURS_M` und `NP_ZIEL_ICON` in denselben
Zeilen bleiben unangetastet.

---

### Hunk 4 — Aufrufstelle in `naechstePunkteAb()` (Zeile 776)

**ALT**
```js
    eintraege: eintraege.slice(0, NP_ANZAHL)
```

**NEU**
```js
    eintraege: eintraege.slice(0, npAnzahl())
```

---

### Hunk 5 — `npStreckenAbstand()`, neu vor `let markerLayer` (Zeile 781)

**ALT**
```js
let markerLayer = [];
```

**NEU**
```js
// Abstand einer Koordinate zur Strecke eines Rennens, in Metern.
// Nur der Abstand, nicht die Streckenposition: map.js waehlt damit
// unter mehreren Teamautos dasjenige aus, das wirklich am Rennen
// haengt. Ein Wagen, der ueber die Bundesstrasse zum Ziel vorfaehrt,
// wird zwar ebenfalls auf die Strecke projiziert - aber eben mit
// einigen Kilometern Abstand.
//
// Bewusst nicht die Streckenposition: auf einem Rundkurs hat der
// Fuehrende direkt hinter dem Zielstrich s ~ 0 und waere damit der
// Hinterste. Nichts hier schreibt.
function npStreckenAbstand(lat, lon, raceId) {
  if (typeof lat !== 'number' || typeof lon !== 'number' || !raceId) return null;
  const coords = gpxByRace[raceId];
  if (!Array.isArray(coords) || coords.length < 2) return null;
  const cum = npCumOf(coords);
  if (!cum) return null;
  const t = sNaechst(lat, lon, coords, cum);
  return t ? t.dist : null;
}

let markerLayer = [];
```

Nutzt ausschließlich vorhandene Funktionen (`npCumOf`, `sNaechst`) und liest
nur. `startOffset`, `marker[]` und `pruefeRundendurchgang` sind nicht berührt.

---

## `version.json`

### Hunk 6

**ALT**
```json
  "version": "2.12.0",
  "date": "2026-09-05",
  "title": "Hauptfeld nach Import"
```

**NEU**
```json
  "version": "2.13.0",
  "date": "2026-09-05",
  "title": "Bezugspunkt Teamauto"
```

`sw.js` braucht keine Änderung — der Cache-Name wird dort aus `/version`
gelesen. Der Bump löst auf jedem Gerät einen Service-Worker-Reload aus, also
**nicht während eines laufenden Rennens hochladen**.

---

# Verifikation

| Prüfung | Ergebnis |
|---|---|
| `node --check map/map.js` | bestanden |
| `node --check map/gpx.js` | bestanden |
| `version.json` parsebar | bestanden |
| Express-Routen `server.js` | 74 → 74 (Datei unverändert) |
| Funktionsnamen `map/map.js` | 55 → 56, neu: `npFrisch` |
| Funktionsnamen `map/gpx.js` | 44 → 46, neu: `npAnzahl`, `npStreckenAbstand` |
| entfernte Funktionen | keine |
| jsdom-Verhaltenstests | 22 von 22 bestanden |
| Puppeteer-Mockup | vor der Umsetzung abgenommen |

Die Tests schneiden die Funktionen aus den ausgelieferten Dateien aus und
führen sie in einer Sandbox aus — geprüft wird der echte Code, kein Nachbau.

**Abgedeckte Fälle:** Vorrang `TEAMAUTO` vor Rolle · Rolle wird genutzt (der
gemeldete Fehler) · Sportler ohne Rolle bleibt unberücksichtigt · Erstwahl über
kleinsten Abstand · Wahl bleibt über mehrere Takte stabil, auch wenn ein
anderer näher kommt · Neuwahl nach Ausfall · Neuwahl nach Rollenentzug ·
veraltete Position wird verworfen · Rückfall auf jüngste Meldung ohne Strecke ·
eigene Ortung hat Vorrang · leere Daten ergeben `null` · Zeilenanzahl bei
sieben Fenstergrößen.

# Bilanz

| Datei | + | − |
|---|---:|---:|
| `map/map.js` | 53 | 9 |
| `map/gpx.js` | 42 | 2 |
| `version.json` | 2 | 2 |
| **Summe** | **97** | **13** |

**Byte-identisch geblieben:** alle übrigen Dateien des Repos, insbesondere
`server.js`, `db.js`, `raceresult.js`, `sw.js`, `index.html`, `css/app.css`,
`race/favorites.js`, `race/taktik.js`, `race/events-ui.js`, `race/displays.js`,
`race/startlists.js`, `core/*`, `map/*` außer den beiden oben.
